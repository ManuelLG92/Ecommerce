$(document).ready(()=>{
    let divImages = document.getElementsByClassName('img-container-checkout')
    const divImagesTotal = document.querySelector(".img-container-checkout");
    divImages = Array.from(divImages);
    console.log(divImages)

  /* divImages.map((div) => div.addEventListener('mouseenter', ()=> div.style.display = 'block'))
    divImages.map((div) => div.addEventListener('mouseleave', ()=> div.style.display = 'none'))*/

/*    divImages.map((div) => div.addEventListener('mouseenter', ()=> div.querySelector('img').style.display = 'block'))
    divImages.map((div) => div.addEventListener('mouseleave', ()=> div.querySelector('img').style.display = 'none'))*/
    function showImage() {

    }
})