<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="title" content="Estilker Colombia">
    <meta name="description"
          content="venta de ceramica de la mejor calidad y diseno hecho en Espana, especialistas en decorados, outlet, ofertas, envio internacional">
    <meta name="keywords"
          content="ceramica, azulejo, importacion, españa, colombia,  decorados, estilker, compra, ventas">
    <meta name="robots" content="index, follow">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="language" content="Spanish">
    <meta name="author" content="Estilker Colombia">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Estilker Colombia SAS{% endblock %}</title>

    {% block stylesheets %}
        <link rel="icon" href="{{ asset('images/logos/estilker_favicon.png') }}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
              integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
              crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/site/publicStyles.css') }}">

        <link href="{{ asset('css/icons/icomoon/styles.css') }}" rel="stylesheet" type="text/css">
        <link href="{{ asset('css/icons/fontawesome/styles.min.css') }}">



    {% endblock %}

</head>
<body>

{% set bathroomId, cuisineId = 6, 5 %}
<div id="snackbar"></div>

<div>
    {{ include('common/_nav_bar.html.twig') }}
</div>

<div class="contact-helper">
    <div class="contact-helper-body">
        <div class="title-contact-helper p-2">
            <span class="text-white font-sofia-bold">Agente</span>
        </div>

        <div class="content-contact-helper shadow p-2">
            <p>Hola 👩 ¿Necesitas ayuda con tu pedido?</p>
            <p>Haz click en el boton de abajo y contacta con nosotros.</p>
        </div>
        <div class="text-center mt-2 mb-2 contact-helper-open-ws">
            <a href="https://api.whatsapp.com/send?phone=573156122776&text=Hola,%20necesito%20ayuda%20con%20mi%20pedido.%20Puedes%20ayudarme?"
            title="Ayuda WhatsApp" class="text-decoration-none p-2 mb-5 link-contact-helper" target="_blank">Abrir WhatsApp</a>
        </div>
        <div class="mt-2">
            <span>&nbsp;</span>
        </div>
    </div>

        <div class="text-end contact-helper-icon">
            <img src="{{ asset('images/logos/whatsapp.png') }}" title="Ayuda" alt="Ayuda"
                 class="ws-helper-img text-decoration-none">
        </div>


</div>

<div class="content">
    <div id="up-arrow">
         <span class="up-arrow">
                <img src="{{ asset('images/logos/icon-arrow-up-circle.png') }}" alt="Up button" class="up-arrow-img">
         </span>
    </div>
    <div>

    </div>
    {# <div class="ws-helper">
        <a href="#" class="text-dark">
            <span class="me-3 shadow p-3 bg-white rounded ws-helper-text">Necesitas ayuda?</span>
            <img src="{{ asset('images/logos/whatsapp.png') }}" alt="Contacto" class="ws-helper-img text-decoration-none">
        </a>
    </div> #}

    <div class="mt-1 body-content">
        {% block body %}

        {% endblock %}
    </div>

</div>
{{ include('common/footer.html.twig') }}

{% block javascripts %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf"
            crossorigin="anonymous"></script>

    <script src="{{ asset('js/core/libraries/jquery.min.js') }}"></script>
    <script src="{{ asset('js/app/site/loginControl.js') }}"></script>
    <script>

        $(document).ready(() => {

            const upArrow = $('#up-arrow')
            $('button[type=reset]').on('click', () => {

                $('#filterForm').find('input[type=checkbox]').each(() => {
                    if ($(this).prop('disabled') !== true) {
                        $(this).prop('checked', false);
                    }
                })
            })

            $(upArrow).hide()
            $(window).scroll(function () {
                if (window.matchMedia("(max-width: 994px)").matches) {
                    $(upArrow).show()
                } else {
                    if ($(this).scrollTop() > 300) {
                        $(upArrow).fadeIn();
                    } else {
                        $(upArrow).fadeOut();
                    }
                }

            });

            $(upArrow).on('click', () => {
                $('html, body').animate({scrollTop: 0}, 800);
            })

            const contactHelperIcon = $('.contact-helper-icon')
            const contactHelperBody = $('.contact-helper-body')
            const linkContactHelper = $('.link-contact-helper')
            contactHelperIcon.add(linkContactHelper).on('click', () => {
                if (contactHelperBody.css('display') === 'none') {
                    contactHelperBody.fadeIn()
                } else {
                    contactHelperBody.fadeOut()
                }
            })
        })
    </script>

{% endblock %}

</body>
</html>
